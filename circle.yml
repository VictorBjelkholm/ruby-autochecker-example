machine:
  services:
    - docker
dependencies:
  override:
    - nvm use v5.5.0
    - npm install -g autochecker
test:
  override:
    - autochecker 2.3.0-alpine 2.1-alpine 2.1.9-alpine 2.2.4-alpine

